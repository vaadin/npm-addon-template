Do not put anything here in add-on projects, even though Vaadin tooling re-creates this directory.

Front-end resources that belong to the add-on should be placed in src/main/resources/META-INF/resources/frontend
